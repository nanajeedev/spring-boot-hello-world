apiVersion: v1
kind: Pod
metadata: 
    name: worker-app
    labels:
      app: worker-app
      type: front-end
spec:
  containers:
    - name: workerapp-container
      image: vsiraparapu/worker-app
      