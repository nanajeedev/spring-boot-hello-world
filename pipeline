pipeline {
    agent any

    stages {
         stage("code_analysis") {
            steps{
                script{
                    sh "ls -lrt"
                    sh "mvn install"
                }
            }
        }
    }
}